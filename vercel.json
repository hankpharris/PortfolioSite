{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "yarn --cwd apps/frontend install && yarn --cwd apps/frontend build",
  "outputDirectory": "apps/frontend/.next",
  "framework": "nextjs",
  "installCommand": "yarn --cwd apps/frontend install",
  "regions": ["iad1"]
}