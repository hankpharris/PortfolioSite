{
    "buildCommand": "yarn install && yarn prisma generate --schema=./prisma/schema.prisma && yarn build",
    "outputDirectory": ".next",
    "framework": "nextjs",
    "installCommand": "yarn install"
} 